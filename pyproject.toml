[project]
name = "reelsi"
version = "0.1.0b0"
description = "AI video editor on top of Premiere and After Effects: multicam cut, word-by-word subtitles, AE assembly"
readme = "README.md"
license = "AGPL-3.0-or-later"
requires-python = ">=3.10"
